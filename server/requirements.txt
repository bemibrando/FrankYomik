manga-ocr
transformers==4.57.6  # v5 breaks manga_ocr's MeCab-based BertJapaneseTokenizer (forces slow->fast conversion, only supports sentencepiece/tiktoken)
pykakasi
opencv-python-headless
Pillow
requests
deep-translator
pyyaml
redis
nodriver
sentencepiece
easyocr
simple-lama-inpainting  # install with: pip install --no-deps simple-lama-inpainting
                        # (its pinned deps conflict with numpy/Pillow/opencv)
pytest
# Optional - for Flux/SDXL inpainting backends:
# pip install diffusers transformers accelerate
